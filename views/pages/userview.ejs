<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% include ../partials/header.ejs %>
</head>

<body>
    <% include ../partials/nav.ejs %>
    <main>
        <div>
            <% if (user) { %>
            <h3><%= user.userid %></h3>
            <p><%= user.username %></p>
            <p><%= user.password %></p>

            <hr>
            <p><a href="/users/destroy?userid=<%= user.userid %>">Delete</a>
                |<a href="/users/edit?userid=<%= user.userid %>">Edit</a></p>
            <% } %>
        </div>
    </main>
</body>

</html>