<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <% include ../partials/header.ejs %>
</head>

<body>
    <% include ../partials/nav.ejs %>
    <main>
        <div>
            <h1><%= title %></h1>
        </div>

        <div class="main-customers">
            <div>
                <table class="table">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Description</th>
                        </tr>
                    </thead>
                    <% for (let i = 0; i < productcategorylist.length; i++) { %>
                    <tr>
                        <td><%= productcategorylist[i]['category_name'] %></td>
                        <td><%= productcategorylist[i]['category_desc'] %></td>
                    </tr>
                    <% } %>
                </table>
            </div>
    </main>
    <% include ../partials/footer.ejs %>
    <% include ../partials/scripts.ejs %>
</body>

</html>